# First real labelled corpus

> **The artifacts are not committed — this directory holds only this file.**
> `.gitignore` excludes `*.jsonl` repo-wide, and the three files described below
> are the output of a GPU sanitizer sweep, not a checked-in fixture. So the
> commands in this file do not run from a fresh checkout until you have built a
> corpus. Two ways to get one, in order of effort:
>
> ```bash
> # (a) No GPU. Six scenarios, from the survey reports committed in this repo.
> #     Enough to exercise every scorer path; smaller and less balanced than the
> #     nine-scenario sweep this file describes.
> python examples/rl/build_corpus.py \
>   --results recipes/sanitizers/survey/reports \
>   --baselines recipes/sanitizers/fixtures/expected/verdict_baselines.json \
>   --out examples/rl/corpus
>
> # (b) The nine scenarios below: run the sanitizer recipes on a gfx950 node
> #     first, then point --results at that run tree.
> ```
>
> This file is kept as the **provenance record** for the nine-scenario sweep the
> measurements in `docs/tokenspeed-rl-e2e-sanitizer-routing.md` were taken
> against, so the numbers there can be read against what produced them. The
> tables below describe that sweep, not whatever you build locally.

Generated by `examples/rl/build_corpus.py` from nine sanitizer runs executed on
a gfx950 compute node (Slurm job 32192, `cv350-rck-g03-c17-08`, partition
`meta64`, ROCm 7.0.2.2). Deliberately not run on the CI runner
`smci350-rck-g03-f16-12`, which is reserved for a nightly baseline window.
Scratch was `/tmp/ts-work-rlcorpus-32192`, distinct from anything CI touches.

RocJITsu bundle `f92da4cb3c5b3612db3752a36f4f3d0d3e9ff768` (rocm-systems run
33641388794); aorta at `ba73dab`.

## Contents

| file | rows | what one row is |
|---|---|---|
| `triage.jsonl` | 9 | one sanitizer run: verdict label, deduped evidence, ground truth |
| `proposal.jsonl` | 45 | one model proposal against a real scenario's candidate set |
| `manifest.json` | — | counts, family split, verdict split, disagreements |

Scored with no conversion pass, once the files exist (see the note at the top):

```bash
python examples/rl/triage_reward.py --corpus examples/rl/corpus/triage.jsonl
python examples/rl/proposal_reward.py --corpus examples/rl/corpus/proposal.jsonl
```

`triage_reward.py` skips rows flagged `ground_truth.agrees = false` — scenarios
where the observed verdict contradicts the committed baseline, so the stored
label is a known tool defect. `--include-disagreements` scores them anyway. All
three baseline-gated cases in this sweep agree, so the flag changes nothing
here; it matters on the next sweep that disagrees.

## Scenarios

| scenario | family | verdict | findings | sites | baseline |
|---|---|---|---|---|---|
| `consan-clean` | synthetic_hip_lds | pass | 0 | 0 | pass, agrees |
| `consan-racy` | synthetic_hip_lds | **fail** | 64 | **1** | fail, agrees |
| `consan-lds-dispatch` | synthetic_hip_lds | pass | 0 | 0 | — |
| `consan-tiny` | synthetic_hip_vecadd | error | 0 | 0 | — |
| `consan-gemm` | tensile_gemm_object | error | 0 | 0 | — |
| `waitcheck` (gated, top-3 CSV) | tensile_gemm_object | warn | 64 | 2 | warn, agrees |
| `waitcheck-gemm` (object) | tensile_gemm_object | warn | 32 | 1 | — |
| `waitcheck-lds-dispatch` | synthetic_hip_lds | pass | 0 | 0 | — |
| `waitcheck-tiny` | synthetic_hip_vecadd | pass | 0 | 0 | — |

All three baseline-gated cases agree with `verdict_baselines.json`.

160 findings, four distinct sites. The racy reproducer alone accounts for 64 of
them and they are one race: same instruction pair (`0x8`/`0x28`), one record per
lane and LDS byte range. Counting findings as examples would inflate this
corpus sixteen-fold, which is why `finding_counts` reports both numbers.

## Observed tool defects

Recorded, not filed — no issues opened for these.

1. **`combined_hook_exit_86` on the caller-supplied-object ConSan path.**
   `consan-tiny` and `consan-gemm` both end `overall_verdict=error`,
   `execution_status=error`, reason `combined_hook_exit_86`, with zero
   findings. Both drive ConSan through `consan_load.hip`, which `hipModuleLoad`s
   a prebuilt `.hsaco`. `consan-lds-dispatch` also supplies its own object and
   passes, so the failure follows the `consan_load` driver rather than the
   caller-supplied object idea in general. Reproducible, not a flake: the six
   archived survey reports show the same three errors.

2. **`Finding.kernel_name` and `Finding.entry_offset` are never populated.**
   0 of 160 findings carry either, on both sanitizers. `code_object`,
   `severity` and `code` are populated on all 160. Kernel attribution is still
   recoverable, but one level up — `kernel_results[].identity.name` and
   `backend.selected_kernel` — so attribution is per-scenario, not per-finding.

3. **ConSan's `code` is not a vocabulary.** Every ConSan finding carries
   `code = "1"`, so the namespaced citation is `consan:1` for all of them. The
   discriminating fields are `severity` (`race`) and the metadata instruction
   pair. Waitcheck by contrast emits the symbolic `wait_hazard`. The corpus
   keeps `code` as the cited token to match `triage_reward`'s existing
   vocabulary and carries `severity` and `metadata` alongside it.
