schema_version: 1
mode: sanitizer
ticket: DEMO-CONSAN-FIXED
description: >
  Same two-wave LDS repro as daily-consan-racy, rebuilt with the __syncthreads()
  barrier in place (expect PASS). Pairing it with the racy recipe shows the
  guardrail flipping on the one-line source change and nothing else.

sanitizer_plan:
  target: gfx950
  source:
    kind: consan_repro
    variant: racy
    hip: fixtures/repro/consan_lds_race_2wave_fixed.hip
    command: fixtures/bin/consan_lds_race_2wave_fixed
    consan_log: true
  scope:
    kind: kernel
  selection:
    requirement: top_dispatch_count
    top_n: 1
  sanitizers:
    - consan
  policy:
    consan_policy: strict
    on_missing_backend: fail
  output:
    report: sanitizer_report.json
